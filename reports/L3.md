# L3 — checkpoint ecosystem: DCP round trip, HF export and fp8 on the decoupled layout

Environment: torch 2.9.1+cu128, transformers 5.14.1, 8×H200 (NGC 25.03 base image).

```bash
PYTHONPATH=. torchrun --nproc-per-node 8 tests/model/run_decoupled_ep_fsdp_ckpt.py \
    --modes A:ep=1 C:ep=8,decouple=1 C4:ep=4,decouple=1 H41:ep=4,decouple=1,hsdp=4 \
    --cross-load C:A A:C C4:H41 --out l3/
```

Same tiny Qwen3-MoE / data / optimizer as L1. Per mode: `from_hf` → `save_hf` (step 0) → 5 steps → `save_dcp` → 5 steps → `save_hf` (step 10); then a fresh engine `load_dcp`s the step-5 checkpoint and trains steps 5–9.

| mode | config |
|---|---|
| A | ep=1, decouple=False, hsdp_sharding_size=None |
| C | ep=8, decouple=True, hsdp_sharding_size=None |
| C4 | ep=4, decouple=True, hsdp_sharding_size=None |
| H41 | ep=4, decouple=True, hsdp_sharding_size=4 |

Noise floor: two independent runs of the same `ep=1` configuration (mode A in L1 and in L2) differ by up to `1.5e-05` (rel, first 10 steps) / `4.2e-05` (50 steps) — the training loop is not bit-deterministic run to run. Differences at or below this level are noise.

## HF export right after `from_hf` vs. the source checkpoint (bf16)

Exercises the HF save mapping of every layout (`_StridedShard` over efsdp + `Shard(0)` over ep for experts; flattened `dp_shard` or `(replicate, dp_shard)` for dense params).

| mode | keys | max abs diff | mismatched / total elements | missing / extra keys |
|---|---|---|---|---|
| A | 807 | 0.000e+00 | 0 / 108139520 | [] / [] |
| C | 807 | 0.000e+00 | 0 / 108139520 | [] / [] |
| C4 | 807 | 0.000e+00 | 0 / 108139520 | [] / [] |
| H41 | 807 | 0.000e+00 | 0 / 108139520 | [] / [] |

## DCP round trip: resume from the step-5 checkpoint and continue

| mode | steps 5–9 continuous | steps 5–9 resumed | max rel loss diff |
|---|---|---|---|
| A | 8.410357, 8.415376, 8.416484, 8.401643, 8.412126 | 8.410357, 8.415341, 8.416493, 8.401610, 8.412211 | 1.02e-05 |
| C | 8.410332, 8.415367, 8.416520, 8.401658, 8.412078 | 8.410332, 8.415341, 8.416520, 8.401702, 8.412045 | 5.22e-06 |
| C4 | 8.410340, 8.415240, 8.416497, 8.401557, 8.412225 | 8.410340, 8.415284, 8.416528, 8.401534, 8.412223 | 5.21e-06 |
| H41 | 8.410381, 8.415386, 8.416442, 8.401473, 8.412083 | 8.410381, 8.415260, 8.416427, 8.401516, 8.412135 | 1.50e-05 |

HF export at step 10 from the resumed engine vs. from the continuous engine (single bf16 ulps on the elements touched by the noise above):

| mode | keys | max abs diff | mismatched / total elements | missing / extra keys |
|---|---|---|---|---|
| A | 807 | 4.883e-04 | 16370836 / 108139520 | [] / [] |
| C | 807 | 4.883e-04 | 16215886 / 108139520 | [] / [] |
| C4 | 807 | 4.883e-04 | 15895028 / 108139520 | [] / [] |
| H41 | 807 | 4.883e-04 | 16056881 / 108139520 | [] / [] |

## HF export at step 10 vs. the `ep=1` baseline export

| mode | keys | max abs diff | mismatched / total elements | missing / extra keys |
|---|---|---|---|---|
| C vs A | 807 | 1.099e-03 | 45633588 / 108139520 | [] / [] |
| C4 vs A | 807 | 1.099e-03 | 45585790 / 108139520 | [] / [] |
| H41 vs A | 807 | 1.221e-03 | 45816998 / 108139520 | [] / [] |

## Bonus: DCP resharding across layouts

The step-5 DCP checkpoint (model + AdamW state) of one layout is loaded into another layout and training continues for steps 5–9; compared with the source layout's own continuous curve.

| src → dst | steps 5–9 after cross load | max rel loss diff vs src continuous |
|---|---|---|
| C → A | 8.410332, 8.415269, 8.416533, 8.401664, 8.412048 | 1.17e-05 |
| A → C | 8.410357, 8.415366, 8.416492, 8.401591, 8.412128 | 6.13e-06 |
| C4 → H41 | 8.410340, 8.415295, 8.416472, 8.401631, 8.412212 | 8.85e-06 |

## Float8 (tile-wise) on the decoupled layout

PR-3 gives routed experts their own fp8 padding chunk count (`efsdp`) and tile-wise reduce meshes with the `efsdp` rank stride, while dense fp8 linears keep `dp_shard`-based ones. Validated on the 3.4B medium model (`--fp8 --model-size medium --seq-len 4096`, 20 steps); the tiny model cannot be used because its fp8 grouped GEMM crashes on the legacy path too (see `reports/decisions.md` D11).

| pair | max rel loss diff (20 steps) | step-0 per-param grad-norm max rel diff: dense / expert |
|---|---|---|
| A vs B | 8.66e-04 | 4.04e-03 / 4.27e-04 |
| C vs B | 6.59e-04 | 8.01e-03 / 6.63e-04 |
| C4 vs B | 2.31e-04 | 3.86e-03 / 5.85e-04 |

fp8 training is noticeably less deterministic than bf16: repeating the *same* configuration twice (`A`/`A2`, `B`/`B2`, `C`/`C2`) gives

| pair | max rel loss diff (20 steps) |
|---|---|
| A vs A2 | 4.84e-04 |
| B vs B2 | 1.46e-04 |
| C vs C2 | 1.04e-04 |
| C vs A | 2.06e-04 |
| C2 vs A2 | 5.66e-04 |

so the decoupled-vs-legacy differences above are inside the fp8 run-to-run noise floor.

Step times with a warm `adaptive_gemm` JIT cache (second run, steps 5–19): A 192 ms, B 242 ms, C 258 ms / C2 251 ms; the first fp8 run of a new shape pays the JIT compile (1906 ms/step average for B, 1899 ms for C4).

Per-rank memory (medium, fp8): A: dense params 105 MiB, peak allocated 9022 MiB; B: dense params 836 MiB, peak allocated 11861 MiB; C: dense params 105 MiB, peak allocated 8203 MiB; C4: dense params 105 MiB, peak allocated 8203 MiB.

## Acceptance (DESIGN §6-L3)

| criterion | result |
|---|---|
| DCP save → load → 5 more steps, loss continuous | all 4 layouts: first resumed step identical, max rel diff ≤ 1.5e-05 (noise floor 1.5e-05) ✅ |
| HF export allclose to the ep=1 baseline | step 0: bit-exact; step 10: max abs diff 1.2e-03 (≤ 1 bf16 ulp) ✅ |
| (extra) DCP reshard across layouts | C↔A, C4→H41 continue within noise ✅ |
| fp8 smoke / numerics | medium model: decoupled vs legacy inside the fp8 run-to-run noise ✅ |
